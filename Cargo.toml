[package]
name = "reversi-ai"
authors = ["curegit"]
version = "1.1.1"
edition = "2024"
license = "Apache-2.0"
readme = "README.md"
publish = false

[lib]
crate-type = ["rlib", "cdylib"]
doctest = false

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
num_cpus = "1.17.0"
